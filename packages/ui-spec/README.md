# Veylumi cross-platform component specification

`components.json` is the portable behavioral specification; each platform owns its native implementation. A component may not introduce raw colors, arbitrary spacing, or a divergent empty/loading/error state. Use `@veylumi/design-tokens` for values and semantic i18n keys for copy.
