# Veylumi iOS

This native SwiftUI app uses the shared REST contract and generated localization resources. Generate the checked-in Xcode project with `xcodegen generate` from this directory when Xcode/XcodeGen are available, then run the scheme's tests on an iOS Simulator.
